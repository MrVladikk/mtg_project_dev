🃏 MTG Коллекция — Magic: The Gathering Collection Manager






Мощное веб-приложение для управления коллекцией карт Magic: The Gathering. Каталогизируйте карты, собирайте колоды, общайтесь на форуме и импортируйте коллекции из CSV — с авто-загрузкой изображений и цен.

✨ Возможности
🗃️ Коллекция

Умный импорт CSV

Распознаёт заголовки на русском/английском.

Автозагрузка изображений: при отсутствии ссылки подтянет картинку из Scryfall API по ID.

Слияние дублей: количество суммируется, а не дублируется запись.

Подробные логи импорта и ошибок.

Поиск и сортировки: по названию, сету, редкости; по цене, алфавиту, новизне.

Карточка карты: крупное изображение, редкость, цена, количество, сет.

⚔️ Конструктор колод

Создание/редактирование колод.

Добавление карт в колоду.

Просмотр состава как сетки.

Поля формата/описания стратегии.

💬 Форум

Темы и сообщения.

Ответы внутри темы.

Базовая модерация (для авторов/админов).

🎨 Интерфейс

Тёмная тема, аккуратные формы и карточки.

Адаптивность (Bootstrap 5).

Уведомления (alerts), удобные формы.

🧱 Технологии

Backend: Django 5.2+, Python 3.12

Frontend: Bootstrap 5, чистый CSS

Хранение медиа: локально (MEDIA_ROOT)

Интеграции: Scryfall API (изображения)

📦 Установка и запуск
Требования

Python 3.10+ (рекомендуется 3.12)

Pip

Git (опционально)

1) Клонирование
git clone https://github.com/ВАШ-USERNAME/mtg-project.git
cd mtg-project

2) Виртуальное окружение
# Windows
python -m venv venv
venv\Scripts\activate

# macOS / Linux
python3 -m venv venv
source venv/bin/activate

3) Зависимости
pip install -r requirements.txt

4) Переменные окружения

Создайте файл .env в корне (по аналогии с .env.example), например:

DEBUG=True
SECRET_KEY=ваш_секретный_ключ_django
ALLOWED_HOSTS=127.0.0.1,localhost

# Опционально, если используете внешние API/прокси
SCRYFALL_API_BASE=https://api.scryfall.com

5) Миграции
python manage.py migrate

6) Суперпользователь
python manage.py createsuperuser

7) Статические файлы (опционально для DEV)
python manage.py collectstatic --noinput

8) Запуск
python manage.py runserver


Откройте: http://127.0.0.1:8000/

⚙️ Настройки статики/медиа (важно)

В settings.py должны быть:

# Статика
STATIC_URL = "/static/"
STATICFILES_DIRS = [BASE_DIR / "static"]            # если используете локальную папку static/
STATIC_ROOT = BASE_DIR / "staticfiles"              # для collectstatic (прод)

# Медиа
MEDIA_URL = "/media/"
MEDIA_ROOT = BASE_DIR / "media"


В корневом urls.py (ТОЛЬКО при DEBUG=True):

from django.conf import settings
from django.conf.urls.static import static

urlpatterns = [
    # ... ваши include(...)
]

if settings.DEBUG:
    urlpatterns += static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT)


Изображения карт ожидаются, например, в media/cards/....
Для моделей с ImageField — используйте {{ obj.image.url }} в шаблоне.

📥 Импорт CSV

Маршрут: /data-processing/upload/ (или свой, если переименовали)
Права: импорт обычно доступен авторизованным пользователям.

Поддерживаемые поля CSV

Обязательные (хотя бы одно из):

scryfall_id (предпочтительно для точного совпадения)

или name + set/set_code

Опциональные:

quantity / qty / количество (по умолчанию 1)

price

foil (true/false)

rarity

collector_number

image_url

Логика импорта (вкратце)

Читаем строку CSV → нормализуем заголовки.

Ищем карту по scryfall_id (если есть) или по name+set_code.

Если карта уже в базе — обновляем, суммируем количество.

Проверяем изображение:

если ImageField пуст → если есть локальный файл в media/cards/ — используем его;

если нет файла → скачиваем с Scryfall по ID и сохраняем.

Записываем результат в лог (кол-во добавленных/обновлённых/ошибок).

Пример заголовков CSV
scryfall_id,name,set_code,quantity,price,foil,image_url
f9d85...,Giant Growth,LEA,2,0.15,false,
,Exsanguinate,CMD,1,6.0,false,https://...

🧭 Основные маршруты (по умолчанию)

/ — главная

/cards/ — список карт

/cards/<id>/ — карточка карты

/sets/ — список сетов

/sets/<id>/ — карточка сета

/decks/ — список колод

/decks/<id>/ — карточка колоды

/forum/ — список тем форума

/forum/thread/<id>/ — тема форума

/auction/ — аукционы (если приложение подключено)

/data-processing/upload/ — импорт CSV (если приложение подключено)

/login/, /register/, /logout/ — аутентификация

Имена URL могут отличаться — смотрите ваши urls.py.

🗂 Структура проекта (пример)
mtg_project/
├─ mtg_project/               # настройки проекта
│  ├─ settings.py
│  ├─ urls.py
│  └─ ...
├─ mtg_app/                   # основное приложение (карты/сеты/колоды)
│  ├─ templates/mtg_app/
│  ├─ static/mtg_app/
│  ├─ models.py, views.py, urls.py, forms.py
│  └─ ...
├─ forum/                     # форум (если подключен)
├─ auctions/                  # аукцион (если подключен)
├─ data_processing/           # импорт CSV (если подключен)
├─ media/                     # медиа-файлы (изображения карт)
└─ static/                    # статические файлы (css/js/img)

🧰 Типичные проблемы и решения

Картинки не отображаются
Убедитесь, что:

MEDIA_URL/MEDIA_ROOT настроены.

В DEV добавлен static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT) в urls.py.

В шаблоне используется порядок: obj.image.url → MEDIA_URL + image_filename → внешняя image_url → плейсхолдер из статики.

TemplateSyntaxError: static('...')
В шаблонах Django нельзя вызывать static(...) как функцию. Нужно:

{% load static %}
<img src="{% static 'mtg_app/img/card_placeholder.png' %}">


NoReverseMatch по namespace
Проверьте, что при include("app.urls") вы указали правильный namespace, а внутри app/urls.py — корректный app_name.

Windows: Pillow / компиляторы
Если проблемы с Pillow, установите готовые колёса:

pip install --upgrade pip wheel
pip install pillow

🤝 Contributing

Форкните репозиторий.

Создайте ветку фичи: git checkout -b feature/AmazingFeature

Коммиты: git commit -m "Add some AmazingFeature"

Пуш: git push origin feature/AmazingFeature

Откройте Pull Request.

📜 Лицензия

Проект распространяется под лицензией MIT.
См. файл LICENSE.